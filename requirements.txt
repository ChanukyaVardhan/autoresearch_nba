# Runtime dependencies for the autoresearch loop + data pull.
# Pinned to lower bounds; newer compatible versions are fine.
numpy>=1.24
pandas>=2.0
requests>=2.28
matplotlib>=3.7

# Only needed to RUN the autoresearch loop (the Codex/OpenAI reasoning agent).
# The data pull, training, backtest, evaluation, and tests do NOT need this.
openai>=1.0
